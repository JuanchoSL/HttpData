<?php

namespace JuanchoSL\HttpData\Tests\Unitary;

use JuanchoSL\HttpData\Containers\Message;
use PHPUnit\Framework\TestCase;

class MessageTest extends TestCase
{
/*
    public function testMessage()
    {
        $message = new Message;
        foreach (['1.0', '1.1'] as $protocol) {
            $this->assertNotEquals($protocol, $message->getProtocolVersion());
            $message = $message->withProtocolVersion($protocol);
            $this->assertEquals($protocol, $message->getProtocolVersion());
        }
    }
*/
}